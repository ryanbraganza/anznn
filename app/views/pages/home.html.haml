- if user_signed_in?
  = render 'responses/survey_response_list'

- else
  - title "Home"
  #content_container
    .container_home
      = form_for("user", url: user_session_path) do |f|
        = render(partial: 'devise/sessions/form', locals: { f: f })

