!!!
%html
  = render partial: "shared/head"
  %body
    #content_wrapper
      = render partial: "shared/topbar"
      .container
        = render partial: "shared/alert_bar"
        - if @title_span12
          %h1.span12= yield :title
        - else
          %h1= yield :title

        = yield :tabs

        = yield
    = render partial: "shared/footer"
