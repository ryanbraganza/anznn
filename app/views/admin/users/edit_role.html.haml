- title "Edit Access Level For #{@user.email}"

= form_for(@user, url: update_role_admin_user_path(@specimen)) do |f|
  = render "shared/error_messages", target: @user
  = render partial: "role_fields", locals: {f: f}
  .actions
    = f.submit "Save"
    = link_to 'Back', admin_user_path(@user)
