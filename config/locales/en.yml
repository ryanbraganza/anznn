# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:

  activerecord:
    attributes:
      response:
        survey_id: 'Registration type'
      batch_file:
        file_file_name: 'File'
        survey_id: 'Registration type'
    errors:
      models:
        response:
          attributes:
            baby_code:
              taken: '%{value} has already been used.'

  unauthorized:
    default: 'You tried to access a page you are not authorised to view.'
